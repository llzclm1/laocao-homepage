# BF-002 P0 回归修复与生产复核（2026-09-13）

## Release Decision

**P0 RELEASE GO**。本次实时复核业务提交 002221c，不以 9 月 10 日历史报告代替当前验收。无 P1 开发。

## 根因与修复

- 旧 Pages 队列卡住问题已解除；修复前 ee28c35 的 GitHub 部署成功，但首页首屏重新加入 For Factories，违反 Buyer 主漏斗边界。
- 删除首屏 Choose your path 整段，保留页脚 Factory 直达链接。未修改 URL、canonical、sitemap、样式或业务提交逻辑。
- 在现有静态测试新增主内容禁止 Factory 链接断言，并在 Pages 上传产物前执行测试。修复前测试失败，修复后通过。

## Buyer 部署与线上版本

GitHub 运行 https://github.com/llzclm1/laocao-homepage/actions/runs/34724075489 ，headSha=002221cb08e5096b2441ac57550c9b7e91202e4a；build、静态验证、artifact upload、Pages deployment 全部 success。

生产首页 main 与此提交本地 build 逐字一致，SHA256：0cf8559a68a05705845fb34d052650295ab1f08cd3ca3213aad1d105840b34a3。完整 HTML 因部署环境分析脚本和 Cloudflare 注入存在差异，不声称全文件相等。

Buyer 首页、Review、Sample Report、before-payment 全部 200，主内容无 Factory 链接。内置浏览器实走 首页 → Review → Sample Report → Review CTA，最终 https://gewuji.dev/supplier-reply-review/before-payment/#start 。桌面导航可见，390px 下 scrollWidth=390；移动端 Review CTA 实际可操作。

## Redirect

https://gewuji.dev/for-factories/?utm_source=bf002&x=a%2Bb 返回 HTTP 301，Location=https://factory.gewuji.dev/?utm_source=bf002&x=a%2Bb；跟随后 200，num_redirects=1。生产 sitemap.xml 返回 200 且不含旧 Factory 首页 URL。本次未改 Cloudflare 规则，无真人验证。

## Factory 与真实收件

当前 Factory 生产部署 42590cbe-5e64-4ea1-840b-de1c589c04ab，对应 main ab0cc21。首页与 /check/ 均 200，主要 CTA 指向本域 /check/。

/check/?release_qa=1&utm_source=bf002_20260913 实走空白公司字段提交，页面显示明确失败；改为 GEWUJI RELEASE QA 测试公司后成功，按钮禁用并显示收件编号 7c111d54-917b-4b92-83d2-5e1b157a6b09。

以数据库 UUID 通过受认证 Wrangler 查询 D1，独立读回同一编号，is_test=1、qualification=pending；不属于真实商机，不联系测试联系人。按数据库名称查询第一次失败，以实际 UUID 查询成功；无需修改权限或后台。

## Analytics 与测试

直接下载当前生产 /check/ 的 JS，确认 submit 在请求前触发；仅 response.ok、result.ok=true、receipt_id 与 request_id 匹配后 success；catch 触发 failed。测试流量使用 internal；事件不含联系资料。当前远程源码的持久化、重试、输入拒绝、存储异常、前端 HTTP/网络/收件不匹配、事件契约测试全部 PASS。

本次验证成功事件语义和生产真实收件，未重新验证 GA 报表处理或合格商机数量。Qualified B2B Lead 仍须 is_test=0 且人工 qualification=qualified；Buyer KPI 保持 Qualified Buyer Submission；mailto/草稿/点击均不可代替收件，付款非 P0 阻塞。

## Git 与后续

业务提交 002221c，fix: restore Buyer-only primary funnel；隔离分支 fix/bf002-p0-closure-20260913 推送 origin/main 成功。保留两个原仓库全部未提交改动。Factory 无业务修改，不重复发布。

P1 未开始；真实案例与合格商机归因不属于本次修复。
